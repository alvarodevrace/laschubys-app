# Gentleman Guardian Angel Configuration
# Provider: opencode — usa la auth del propio opencode (sin API key aparte)
PROVIDER="opencode"

# Defaults activos: FILE_PATTERNS="*", RULES_FILE="AGENTS.md", STRICT_MODE="true", TIMEOUT="300"
